import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Authentication, getCurrentSession } from '@/api/authentication'
import { IfLoggedIn, IfNotLoggedIn } from '@/component/authentication'

const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
  fallback: ["inter", 'arial'],
})

export const metadata: Metadata = {
  title: 'Simple Next Auth Looking Sick!',
  description: 'Generated by create next app & you can do this!',
}

export default async function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {

  return (
    <html lang="en">
      <body className={ inter.className }>
        { children }
      </body>
    </html >
  )
}

